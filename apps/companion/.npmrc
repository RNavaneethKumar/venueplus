# This package is managed by npm (not pnpm) and is intentionally outside the
# pnpm workspace so that electron-builder gets a plain flat node_modules.
#
# workspaces=false prevents npm from walking up to the monorepo root and
# trying to resolve workspace: protocol deps from other packages.
workspaces=false
engine-strict=false
